package com.nenkov.bar.application.discount.service;

/**
 * Feature façade for discount-related workflows.
 *
 * <p>This is the single public entry point for the discount feature. It should remain thin and
 * delegate workflow logic to handlers.
 */
public interface DiscountService {
  // Intentionally empty for now.
  // We will decide in 3.2.2.5 whether discount orchestration belongs in application layer at this
  // time.
}
